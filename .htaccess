RewriteEngine On

# If the request is not for a file or directory
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Rewrite to index.php with route parameter
RewriteRule ^(.*)$ index.php?route=$1 [QSA,L]